# MILK10k CBM: 매뉴스크립트 자체 보고 성능 vs 실제 리더보드 결과 비교

- 대상 코드: 이 저장소 자체 (`baseline/pbanavara-milk10k`, submodule `enjoeyland/pbanavara-milk10k`)
- 대상 논문: [`outputs/manuscript.pdf`](outputs/manuscript.pdf) — "Interpretable Skin Lesion Classification via Concept Bottleneck Models on the MILK10k Dataset" (Pradeep Banavara)
- 리더보드 제출: MILK10k 챌린지 공식 리더보드, 팀 "Rekura" — "Concept bottleneck model for skin lesion classification" (설명 문구가 매뉴스크립트 abstract와 일치해 동일 제출로 간주함. 팀명-저자명 매핑은 사용자가 제공한 리더보드 화면 캡처 기준이며 별도 확인은 못함)

## 1. 세 숫자

| | Macro F1 (= Dice Coefficient) | 산출 방식 |
|---|---|---|
| 매뉴스크립트 자체 보고 (Table 1 row 10, `Results`) | **0.5129** | 학습 데이터 5,240 lesion을 자체적으로 80/20 patient-level stratified split, 그 20%(약 1,048 lesion) 검증셋 기준. Phase 2 50 epoch 중 early stop epoch 37 체크포인트 |
| **우리 재현 실행** (이 저장소 코드 그대로, job 1986761) | **0.5290** | 동일 코드/설정(`configs/default.yaml`, seed=42, val_split=0.2)을 우리 쪽에서 그대로 재실행. Early stop epoch 41 |
| 공식 리더보드 (Rekura) | **0.469** (Balanced Multiclass Accuracy 0.501) | MILK10k 챌린지 공식 held-out test set 기준 |

**격차 (공식 리더보드 vs 매뉴스크립트 자체 보고): -0.044 (상대 약 8.5% 하락)**. 두 자릿수 퍼센트로 무너지는 수준은 아니지만, "자체 val 점수를 그대로 믿고 제출 등급을 예상하면 안 된다"는 걸 보여주는 정도의 갭.

**재현성 확인**: 같은 코드/설정을 우리가 독립적으로 재실행했을 때 0.5290이 나와서, 매뉴스크립트의 0.5129가 fluke나 과장이 아니라는 게 확인됨 — 오히려 우리 재현치가 더 높음. 즉 두 번의 서로 다른 실행이 전부 0.51~0.53 구간에 몰려 있고, 리더보드의 0.469 근처로는 전혀 안 내려감. 이건 아래 3절의 원인 후보 중 "모델 선택이 자체 val에 우연히 잘 맞았을 뿐"이라는 설명보다, "train 데이터 자체와 공식 test set의 분포가 다르다"는 설명 쪽에 더 힘을 실어줌.

## 2. 클래스별 비교

리더보드가 제공한 클래스별 Dice Coefficient, 매뉴스크립트 Table 2(자체 val F1, best checkpoint), 우리 재현 실행의 per-class F1(best checkpoint, `outputs/train_20260809_024032.log`)을 나란히 놓으면:

| Class | 매뉴스크립트 val F1 | 우리 재현 val F1 | 공식 리더보드 Dice | Δ (공식 − 매뉴스크립트) | Train N |
|---|---|---|---|---|---|
| BCC | 0.859 | 0.859 | 0.647 | **-0.212** | 2,522 |
| MEL | 0.615 | 0.568 | 0.414 | **-0.201** | 449 |
| BEN_OTH | 0.353 | 0.429 | 0.250 | -0.103 | 44 |
| NV | 0.785 | 0.792 | 0.710 | -0.075 | 745 |
| VASC | 0.636 | 0.583 | 0.615 | -0.021 | 47 |
| MAL_OTH | 0.000 | 0.000 | 0.000 | 0.000 | 9 |
| BKL | 0.477 | 0.450 | 0.505 | +0.028 | 543 |
| AKIEC | 0.544 | 0.557 | 0.592 | +0.048 | 302 |
| SCCKA | 0.685 | 0.676 | 0.737 | +0.052 | 473 |
| DF | 0.400 | 0.455 | 0.471 | +0.071 | 52 |
| INF | 0.105 | 0.231 | 0.222 | +0.117 | 50 |

가장 눈에 띄는 건 **BCC와 MEL(흑색종)이 공식 테스트에서 가장 크게 떨어졌다**는 점 — 각각 매뉴스크립트에서 가장/두 번째로 흔한 클래스(2,522개, 449개)이자, MEL은 임상적으로 가장 중요한 오진 리스크 클래스. macro F1이라는 단일 숫자만 보면 8.5% 하락 정도로 보이지만, 정작 "많이 틀리면 안 되는" 두 클래스에서 자체 보고치 대비 20%p 이상 깎였다는 게 이 비교에서 나온 가장 실질적인 시사점. 특히 **BCC는 우리 재현치와 매뉴스크립트가 0.859로 소수점까지 완전히 일치**하는데도 리더보드에서는 0.647까지 떨어져서, 이 클래스만큼은 "우연한 val split 변동"보다 "학습 데이터엔 없는 패턴이 공식 test set에 있다"는 설명이 더 설득력 있음.

반대로 DF/INF/AKIEC/BKL/SCCKA 같은 중소형 클래스는 매뉴스크립트·우리 재현·리더보드 세 값 모두 비슷한 범위(±0.1 이내)에서 오히려 리더보드가 더 잘 나오거나 비슷했음.

## 3. 왜 갭이 생겼을까 (가설, 검증은 못함)

1. **분포 차이(train-val split vs 공식 held-out test)**: 매뉴스크립트의 "20% 검증셋"은 어차피 같은 5,240-lesion 학습 데이터에서 뗀 것이라 촬영 장비/기관/전처리가 train과 사실상 동질적. 공식 test set은 별도로 수집된 lesion들이라 분포가 다를 가능성이 크고, 특히 BCC/MEL처럼 시각적 이질성이 큰 클래스에서 이런 shift에 더 취약할 수 있음.
2. **희귀 클래스의 표본 변동성**: MAL_OTH(9), VASC(47), INF(50), DF(52), BEN_OTH(44) — 클래스당 val 표본이 수십 개 수준이라 F1 자체의 표준오차가 크다. 이번 비교에서 이 클래스들이 오히려 개선된 것도 실제 모델 성능 차이라기보다 표본이 작아 생기는 노이즈일 가능성을 배제 못함.
3. **Early stopping / 모델 선택이 자체 val set에 맞춰짐**: patience=7, macro F1 기준으로 50 epoch 중 최적 체크포인트(epoch 37)를 골랐는데, 이 선택 과정 자체가 (직접 학습에 쓰지 않았어도) 그 1,048개 val lesion에 암묵적으로 맞춰지는 효과가 있음 — 반복적으로 같은 val set에 대해 체크포인트를 고르면 어느 정도 optimistic bias가 낀다는 건 일반적으로 알려진 현상.
4. **출력 형식 차이**: 매뉴스크립트는 최종 제출에 softmax 대신 sigmoid(클래스별 독립 확률)를 썼다고 명시(`5. Discussion — Submission format`). 공식 채점기가 argmax/threshold를 어떻게 적용하는지에 따라 자체 평가 스크립트와 미묘하게 다른 결정 규칙이 적용됐을 가능성.

**우리 재현 실행(0.5290)이 3번 가설의 비중을 줄여줌**: 만약 갭의 주된 원인이 "그 특정 val split/체크포인트에 우연히 잘 맞았다"는 것이었다면, 같은 코드를 다시 돌렸을 때 결과가 리더보드 쪽(0.469)에 좀 더 가깝게 나오거나 최소한 매뉴스크립트 값과 눈에 띄게 벌어져야 할 텐데, 실제로는 0.5129 → 0.5290으로 오히려 더 높게 재현됐음. 두 독립 실행이 이렇게 좁은 범위에 몰려 있다는 건 "이 코드/설정으로 학습 데이터의 20% val split에서는 안정적으로 0.51~0.53이 나온다"는 뜻이고, 리더보드의 0.469는 val split의 우연이 아니라 **학습 데이터와 공식 test set 사이의 실제 분포 차이(1번 가설)**로 설명하는 쪽이 더 타당해짐.

결론적으로 1번(분포 shift)이 가장 유력한 원인이고, 3번(모델 선택 bias)은 부분적으로는 있겠지만 갭의 주된 설명은 아닌 것으로 보임. 다만 공식 테스트셋 자체나 채점 로직에 접근할 수 없어 완전히 확정할 수는 없음.

## 4. 참고: 우리 자체 baseline과의 위치

같은 MILK10k 11-class(`isicdx_ISIC2025`) 태스크에서 우리가 `baseline/MILK10k_train_base`(ResNet50, non-CBM)로 돌린 5-fold CV 결과는 macro F1 ≈ 0.366 (자체 val 기준, [`260808 학습 로그`](../MILK10k_train_base/runs/20260808_225502/training.log) 참고). 세 지점을 순서대로 놓으면:

```
우리 baseline (자체 val, ResNet50)        0.366
Rekura CBM 공식 리더보드                   0.469
Rekura CBM 자체 보고 (매뉴스크립트)         0.5129
Rekura CBM 우리 재현 (자체 val, 동일 코드)  0.5290
공식 리더보드 1위 (Allegoria 3D-ViT v3)     0.705
```

CBM 접근이 우리 단순 baseline보다는 확실히 낫고(+0.10), 공식 최상위권과는 아직 거리가 있다(-0.24). "자체 val 점수"만 보고 판단하면 실제 순위를 과대평가하게 된다는 게 이번 비교의 핵심.
